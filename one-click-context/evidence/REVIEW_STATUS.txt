ONE CLICK CONTEXT 0.7.0 — validation 2026-09-09

27 browser test groups PASS; 0 FAIL; 1 BLOCKED_ENV; 1 NOT_RUN.
The production region selector and content capture ran in Google Chrome 152 on
local synthetic DOM. New cases cover navigation + chat + right document, explicit
chat/document/combined modes, multiple-document choice, synthetic-click rejection,
nested documents, iframe non-reading, editor coverage warning, stale selection,
open shadow DOM, and the absence of a broad body fallback.

32 background orchestration/security tests PASS with mocked Chrome APIs.
21 native host/client tests PASS with mocked Codex process boundaries.
17 production converter/library UI tests PASS in real Chrome; extension and native
APIs are mocked in that suite. JavaScript syntax and git diff checks PASS.

BLOCKED_ENV: Chrome did not expose the loaded-extension service worker to Playwright,
so the real offscreen clipboard integration was not repeated for 0.7.0. The same
check passed in the earlier environment recorded by 0.2.0 evidence, but historical
success is not reported as a current PASS.

NOT_RUN: signed-in ChatGPT/Codex/DeepSeek DOM and a physical toolbar/context-menu
click in the user's normal profile. The tests do not claim site-specific adapters,
complete virtual-editor contents, frame access, original document bytes, or hidden
server history. Manual installed-extension acceptance is still required.

No new host permission, clipboardRead, debugger, cookies, history, all_urls,
network request, Supabase write, AI call, or trading action was added.
